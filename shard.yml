name: embrace-crystal
version: 2.0.11

authors:
  - Wolfgang Mayerle <wolfgang.mayerle@h3o.de>

# use `shards build` or `shards build --release` to generate into bin/
targets:
  embrace:
    main: src/gui/embrace_main.cr

crystal: ">= 1.20.0"

license: AGPL-3.0-only

dependencies:
  crymble-ui:
    github: wolfgang371/crymbleui
    version: ~> 0.6.0
  crexcel:
    github: wolfgang371/crexcel
    version: ~> 0.1.1
  xlsx-parser:
    github: d1ceward/xlsx-parser
    version: ~> 1.4.0
  debug:
    github: Sija/debug.cr
    version: ~> 2.0.4

# development_dependencies:
#   crystal-kcov:
#     github: vici37/crystal-kcov
